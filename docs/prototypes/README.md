# prototypes

HTML prototypes for features whose SPEC declares `Applies: [...prototype...]`. `ulm` is a terminal CLI — most features will not have prototypes here. Kept as a folder so the doc tree stays canonical.
