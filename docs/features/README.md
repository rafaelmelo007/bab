# features

One folder per feature, slug-named. Each folder holds at minimum SPEC.md, TASKS.md, DECISIONS.md, SCORE.md. Conditional files (DBSCHEMA, INTERFACE-CONTRACTS, CLAUSES, prototypes/) appear when declared in the SPEC's `Applies:` field.

See `docs/bundle/README.md` "Per-feature anatomy" for the full reference.
