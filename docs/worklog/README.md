# worklog

Auto-populated by the Stop hook (`docs/bundle/scripts/worklog-stop-hook.sh`) at the end of every Claude Code turn. One file per day, named `YYYY-MM-DD.md`. One row per turn.

Empty until the first turn fires. See spec §5.2 for the format and §5.3 for the weekly overhead budget.
