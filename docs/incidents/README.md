# incidents

Security findings and post-mortems land here, one file per incident, named `YYYY-MM-DD-<slug>.md`. Populated by `/vskit:security-review` and by hand after production incidents.

Empty until the first incident.
